<?php
/**
 * Custom Fields (Meta Boxes natius)
 *
 * En aquest tema els camps personalitzats es gestionen amb ACF
 * (vegis `inc/acf-fields.php` i `inc/acf-fields/`). Aquest arxiu es
 * conserva com a hook per si en el futur cal registrar meta boxes
 * addicionals sense dependre d'ACF.
 *
 * @package Ave_Maria
 */

defined( 'ABSPATH' ) || exit;

// (Sense meta boxes natius per ara — tot es gestiona via ACF.)
